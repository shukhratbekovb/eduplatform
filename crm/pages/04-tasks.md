# Страница 4: Задачи

**Файл:** `crm/src/app/(crm)/tasks/page.tsx`  
**Компоненты:** TaskKanban, TaskForm  
**Статус:** 🔴 Нужны правки

---

## Найденные проблемы

### 1. Календарный вид — заглушка
```tsx
<p className="text-sm text-gray-400 text-center py-8">
  Календарный вид будет добавлен в следующей версии.
</p>
```
- По PRD §5.5 нужен Calendar view (day/week/month)
- Кнопка есть, переключение работает, но содержимое — пустое

**Фикс:** реализовать или скрыть кнопку календаря до готовности

---

### 2. Нет фильтра по менеджеру в Kanban
- Директор должен видеть задачи всех менеджеров с фильтром
- Чекбокс "Все менеджеры" есть, но нет выбора конкретного менеджера
- `showAll = true` — показывает всех, но без возможности выбрать одного

**Фикс:** добавить `<select>` менеджера когда `isDirector && showAll`

---

### 3. Overdue задачи не выделены визуально на уровне страницы
- Канбан-колонка Overdue есть, но нет счётчика/алерта наверху страницы
- PRD §5.9: виджет "Overdue Tasks — Count + list"

---

### 4. Нет обработки ошибок
- `useTasks()` — нет `isError` branch

---

## Исправления

- [ ] Calendar view — реализовать базовую версию или убрать кнопку
- [ ] Фильтр по конкретному менеджеру для Директора
- [ ] Счётчик просроченных задач в header
- [ ] Обработка ошибок загрузки
